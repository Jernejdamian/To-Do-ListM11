import "./style.css";

const Form = () => (
    <form className="form">
        <input placeholder="Jakie jest następne zadanie?" />
        <button className="form__button">Dodaj zadanie</button>
    </form>
);

export default Form;

